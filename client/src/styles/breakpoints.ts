const sm = 480;
const md = 768;
const lg = 1024;
const xl = 1280;

export const breakpoints = {
  sm: `@media(max-width: ${sm}px)`,
  md: `@media(max-width: ${md}px)`,
  lg: `@media(max-width: ${lg}px)`,
  xl: `@media(max-width: ${xl}px)`,
};
